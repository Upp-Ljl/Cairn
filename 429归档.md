# 429 会话归档

> 日期：2026-04-29（全天）
> 分支：main，HEAD `2320c03`
> 提交范围：`d44c44e..2320c03`（5 commits，全部 docs/poc 类，无代码改动）
> 阶段：v2 方向转向日——从"工具集"到"多 agent 协作内核（Agent OS）"；7 个阶段；决策 20-34（从决策 20 接续 428 归档的决策 19）

---

## 1. 一句话总结

v1 → v2 方向转向落锁（Agent OS / 三动词 / 四能力），PRODUCT.md 从 617 行重写至 1319 行，ARCHITECTURE.md 从零起至 705 行，PoC-1 和 PoC-2 当天完成并通过，W3 计划 + README v2 双轨发布，commit 粒度从 6 压缩为 3 后 force push；今天**没有改任何代码**——全部是文档、验证脚本和设计决策。

---

## 2. 时间线

### 阶段一：v1 → v2 定位转向（早间，无 commit）

**触发**：用户提出"打算把 cairn 做成带模型的守护进程类产品"，随即展开定位讨论。

关键对话链：

1. 我给"自带模型 daemon"定位草案（一句话 / 三支柱 / 四能力 / 与友商对照），保留 v1 痕迹。
2. 用户校正：模型在什么时候介入？——我列出 9 个触发时刻 + 三档预设。
3. 用户进一步校正："不做用户显式喊话的回复……我们做的是用户和本机 agent 之间的协作管理者，类似 Windows 系统。"——这一句话锁定了 **Agent OS** 核心定位。
4. 用户补边界："用户喊话也可以有，但只限简单回复 + 类似搜索给结果，不帮用户改代码。"——锁定 **Inspector 通道**（只读 + 元信息）。
5. 用户定策略："B 引导 + C 派单结合，把用户需求转化为给 agent 的指导。"——锁定**红线策略**（Cairn 自己不修代码）。

**决策 20**：Cairn v2 = 多 agent 协作内核（Agent OS），不写代码，不是 agent。
**决策 21**：三动词锁定——Dispatch / Rewind / Arbitrate。
**决策 22**：四能力锁定——冲突可见 / 状态可逆 / 需求可派 / 消息可达。
**决策 23**：用户接 Cairn 两条通道——Inspector（只读 NL 查询）+ 直接命令（rewind / list / delete 走原 8 工具）。
**决策 24**：红线策略 = B 引导 + C 派单结合；硬底线"Cairn 自己不修代码"。

---

### 阶段二：PRODUCT.md v2 起稿 + 4-persona stress-test（中段，commit `5d8046f` 包含）

**触发**：用户指令"先动 PRODUCT.md v2，把这版定位 + 三动词 + 四能力落成正式文档，原 v1 内容降级到附录。"

执行过程：

- dispatch sonnet subagent 起 v2 主体（§0~§17 + 附录 §18 v1 全文 + §19 变更记录），共 1319 行。
- sonnet 留 3 条 TODO，我处理：补 §10.5 v2 北极星（4 句标志性自发复述句式）+ 附录开头加节号说明（v1 §X 与 v2 §X 不连续）。
- 删除临时 backup / draft，保留 PRODUCT.md 主文件。

**4-persona stress-test**：

用户提议用 4 个 persona 对 PRODUCT.md 做用户模拟——我建议用 subagent 模式代替手写。dispatch 4 只 sonnet：

| Persona | 类型 | 5 个硬问题方向 |
|---|---|---|
| 张涛 | 核心 ICP（CC 重度用户） | 装机理由 / 痛点 / workaround / 共鸣&turnoff / Cairn 消失的反应 |
| Jess | 次要 ICP（Cursor + Cline） | 同上 |
| Klaus | anti-ICP（单 agent 极简主义） | 同上 |
| Marco | 边缘（学生 / 副业） | 同上 |

Stress-test 暴露 12 条 gap，按 P0 / P1 / P2 排序。用户选 P0（5 条），sonnet 落地：
- US-A 真实场景（张涛 `shared/types.ts` 案例写入 §5.2）
- US-S 真实场景（Jess Stripe v3/v2 subagent 消息丢失案例写入 §5.4）
- §5.1.1 Arbitrate 时间粒度 4 档
- §9.2 三种 daemon observe 路径
- §5.4 反汇总边界

我另做 2 处一致性 TODO（§4.6 AC US-A "30 秒"→"1 秒 on MCP-call"；§5.1 表格"进程总线感知"→"MCP-call 边界感知"）。

**决策 25**：4-persona office-hours stress-test 作为产品文档质量门槛（持久制度），后续每次大改 PRODUCT.md 都重跑一轮。

---

### 阶段三：ARCHITECTURE.md + pre-impl-validation 双轨并行（中段，commit `5d8046f`）

**触发**：用户指令"基于当前产品文档，接下来设计具体实现 + 做前验证。"经过选型讨论后，用户选 C（双轨并行）。

dispatch 两只 sonnet 并行：

- **A** 起 `ARCHITECTURE.md`（705 行，含 8 个 🚧 锚点：PoC-1~4 + D-1~4，含系统全景图 / 四能力数据流 / 7 个 ADR）
- **B** 起 `docs/superpowers/plans/2026-04-29-pre-impl-validation.md`（约 500 行，4 PoC + 4 调研项 + 决策矩阵 + 时间盒）

两份文档 cross-ref 编号一致（PoC-1~4 / D-1~4 共享）。

**决策 26**：pre-impl-validation 流程化（4 PoC + 4 调研）作为 v0.1 编码前的硬验证闸。

---

### 阶段四：第一波 PoC（PoC-1 + PoC-2，commit `555dde0`）

#### PoC-1：race window 压测

脚本：`packages/daemon/scripts/poc-1-race-stress.mjs`（worker_threads 多 connection 并发 SQLite 写）

4 场景结果（各 1000 ops）：

| 场景 | N | success | SQLITE_BUSY | p99 ms | 综合 |
|---|---|---|---|---|---|
| baseline | 2 | 100% | 0 | 1.38 | PASS |
| modest | 5 | 100% | 0 | 3.92 | PASS |
| elevated | 10 | 100% | 0 | 5.91 | PASS |
| stress | 50 | 100% | 0 | 448.59 | FAIL on p99 |

细化 verdict：v0.1 ICP 并发上限 N≤10 全部 PASS；N=50 的 449ms 是 SQLite 单写者锁的架构天花板，记录为 v0.3 议题（跨机 / 共享 daemon / agent 市场场景需重新评估 storage backend）。

落 `docs/superpowers/plans/2026-04-29-poc-1-results.md`，回填 ARCHITECTURE.md 4 处 PoC-1 锚点。

**决策 27**：SQLite WAL + busy_timeout=5000ms 在 v0.1 ICP 内足够；conflict-detection 应用层可直接叠加。

#### PoC-2：git pre-commit hook 延迟

脚本：`packages/daemon/scripts/poc-2-conflict-check.mjs`（hook 本体）+ `poc-2-hook-bench.mjs`（bench harness）

5 场景结果：

| 场景 | staged paths | exit≠0 | p99 ms | 阈值 ms | 综合 |
|---|---|---|---|---|---|
| clean | 0 | 0 | 81.4 | 200 | PASS |
| small | 10 | 0 | 86.0 | 200 | PASS |
| medium | 100 | 0 | 85.3 | 200 | PASS |
| large | 1000 | 0 | 122.2 | 1000 | PASS |
| fail-open | DB 缺失 | 0 | 88.3 | — | PASS |

关键发现：Node 冷启动 ~70ms 是延迟主导；SQLite 边际成本 ~45μs/query（prepared statement 复用）；fail-open 路径 24ms 内 exit 0，架构承诺成立。

落 `docs/superpowers/plans/2026-04-29-poc-2-results.md`，回填 ARCHITECTURE.md 2 处 PoC-2 锚点。

**决策 28**：commit-after hook 路径在 v0.1 完全可用；Native 二进制（消除 Node 冷启动 ~70ms）记为 v0.2/v0.3 候选。

---

### 阶段五：Floating Marker 形态决议（中后段，commit `ffd3e8b`）

**触发**：用户指令"设计悬浮标，类似桌面宠物但本质是独立应用进程，桌面右下角。"

关键确认：这**不是** v1 桌面宠物的回潮——v2 悬浮标是 Inspector 通道的视觉载体（静物 / 不接需求 / 不写代码）。

我给 5 轴设计草案（形态 / 视觉 / 信息层级 / 技术栈 / 新增包）+ 4 个待拍板题，用户选"按你推荐"：

- **路标隐喻**（cairn 石堆 / waymark 路标）
- **技术栈 Tauri**（Rust + WebView，~5-10MB，启动 < 200ms）
- **用户首启选可见性**（标准 / 精简 / 极简三档）
- **现在动文档**

dispatch sonnet 落 9 处编辑：§8 重构成 8.1 v0.1 回退方案（状态栏图标）/ 8.2 v0.2 主形态（Floating Marker）/ 8.3 防退化反模式；并更新 §1.3 反定义 / §6.2 N-3 / §10.3 / §11 / §14 / §3.3；ARCHITECTURE.md ADR-8 新增（Tauri vs Electron vs Native）。

我做 1 处微调（§8.2 引子去重）。

**决策 29**：Floating Marker = v0.2 主 UX 形态（取代 v1 桌面宠物 + 取代 v2 §8 状态栏图标候选）。
**决策 30**：技术栈 ADR-8 = Tauri（Rust + WebView，~5-10MB，启动 < 200ms）。

---

### 阶段六：W3 plan + README v2（commit `ffd3e8b`，与阶段五同 commit）

**触发**：用户指令"起 W3 plan + 更新 README，调用 sonnet。"

两份文档发现均需整体重写：

- 现有 `2026-05-04-wedge-w3.md`（原 384 行）是 v1 路线（"W4 路径决策" / "7 工具" / 旧 §17.3 成功判据）
- 现有 README（283 行）是早期"Undo for agents / proxy / Recorder-Classifier-Reverter"框架

dispatch 两只 sonnet 并行：

- **A** 重写 W3 plan（469 行，5 day-by-day 任务 + 8 条 W4 启动 gate + 失败应对表 + §4 W3 完成判据 8 条）
- **B** 重写 README（339 行，新 tagline + 四能力 + 三动词 + 反定义 7 条 + Floating Marker 预告 + PoC-1/PoC-2 数字内联）

**决策 31**：W3 = 前置验证收尾周（PoC-4 dogfood + D-1/D-2 调研 + W4-W7 plan 起草），不进编码阶段。

---

### 阶段七：PoC-3 / PoC-4 prep + commit 粒度修正（commit `138e43e` + `2320c03`）

**PoC-3 + PoC-4 prep 双轨**：

dispatch 两只 sonnet 并行：
- **A** 起 `docs/superpowers/plans/2026-04-29-poc-3-prep.md`（561 行，20 NL × 4 类 × 5 维度 rubric + 决策矩阵，单 provider 版本）
- **B** 起 `docs/superpowers/plans/2026-05-04-poc-4-tracking.md`（550 行，.mcp.json 4 平台配置 + 5 推荐任务 + 17 expected subagent 样本 + system prompt 模板 + 空记录表）

`138e43e` 落地。

**commit 粒度修正**：

用户反馈"commit 太多了"（今天已有 6 个），选 B（让我 squash）。

流程：
1. 建 backup branch `backup/pre-squash-2026-04-29`
2. `git reset --soft d44c44e`（软回 W2 EOD baseline）
3. 重新分 3 个 logical commit + `ffd3e8b` / `138e43e` 复用：

| 新 SHA | message | 内容 |
|---|---|---|
| `5d8046f` | `docs(spec): PRODUCT.md v2 (Agent OS pivot) + ARCHITECTURE + validation plan` | PRODUCT.md v2 + ARCHITECTURE.md + pre-impl-validation |
| `555dde0` | `test(poc): pre-impl validation — PoC-1 race window + PoC-2 git hook` | 两个 PoC 脚本 + 报告 + ARCHITECTURE 锚点回填 |
| `ffd3e8b` | `docs(spec): Floating Marker (v0.2 main UX) + ADR-8 + W3 plan + README v2` | Floating Marker + ADR-8 + W3 plan + README |
| `138e43e` | `docs(poc): PoC-3 NL prompt deck + PoC-4 dogfood tracking template` | PoC-3 prep + PoC-4 tracking |

force push with-lease 失败（本地和 remote 历史已分叉）→ 改 force push 成功。

**ADR-4 重写**（`2320c03`）：

用户校正"我用 MiniMax 的 coding plan，因为做测试比较便宜"——触发 ADR-4 重写。

ADR-4 从"Sonnet vs local 7B"改为 **LLM provider-agnostic 接口**（OpenAI-compatible 抽象，`~/.cairn/config.json` 配置 provider + endpoint + API key + model）。PoC-3 同步升级为双 provider 测试（推荐 MiniMax + DeepSeek，时间盒 60-120 分钟）。

**决策 32**：ADR-4 重写 = LLM provider-agnostic 接口（OpenAI-compatible 抽象，不锁定单一供应商）。理由：项目维护方用 MiniMax / DeepSeek（成本敏感 + 中国数据驻留），锁定 Anthropic 与"开源 + 本地优先 + 成本敏感"调性冲突。
**决策 33**：PoC-3 升级为双 provider 测试（MiniMax + DeepSeek，各跑 20 NL prompt，按 5 维度打分）。
**决策 34**：commit 粒度规则 = 按"逻辑工作单元"，不按"完成一个文件"。今天 6 → 3 落地，后续严守。

---

## 3. 交付物清单

### 文档（今日新建或全量重写）

| 文件 | 行数 | commit | 说明 |
|---|---|---|---|
| `PRODUCT.md` | 1319 | `5d8046f` | v2 重写（§0-§17 + §18 v1 附录 + §19 变更记录）；4-persona 5 条 P0 + Floating Marker / §5.1.1 / §9.2 / §5.4 更新（`ffd3e8b`） |
| `ARCHITECTURE.md` | 705 | `5d8046f` | 0 → 705 行（系统全景图 / 四能力数据流 / 7 ADR / 8 🚧 锚点）；PoC-1/PoC-2 锚点回填（`555dde0`）；ADR-8 + §10.2 等（`ffd3e8b`）；ADR-4 + PoC-3 锚点更新（`2320c03`） |
| `README.md` | 339 | `ffd3e8b` | 283 → 339 行（全量重写，v2 tagline + 四能力 + 三动词 + 反定义 + PoC 数字） |
| `docs/superpowers/plans/2026-04-29-pre-impl-validation.md` | ~500 | `5d8046f` | 4 PoC + 4 调研项 + 决策矩阵 + 时间盒（W3-W4 编码前硬验证闸） |
| `docs/superpowers/plans/2026-04-29-poc-1-results.md` | 201 | `555dde0` | PoC-1 结果报告（N=2/5/10/50 数据，PASS in v0.1 ICP） |
| `docs/superpowers/plans/2026-04-29-poc-2-results.md` | 188 | `555dde0` | PoC-2 结果报告（5/5 PASS，p99 max 122ms，fail-open 验证） |
| `docs/superpowers/plans/2026-05-04-wedge-w3.md` | 469 | `ffd3e8b` | W3 plan 全量重写（v2 路线，5 day-by-day + 8 W4 gate + 失败应对） |
| `docs/superpowers/plans/2026-04-29-poc-3-prep.md` | 561 | `138e43e` | PoC-3 NL prompt deck（20 prompt × 5 维度 rubric，双 provider 版） |
| `docs/superpowers/plans/2026-05-04-poc-4-tracking.md` | 550 | `138e43e` | PoC-4 dogfood tracking（.mcp.json 配置 + 5 任务 + 记录表模板） |

### 脚本（新建）

| 文件 | commit | 说明 |
|---|---|---|
| `packages/daemon/scripts/poc-1-race-stress.mjs` | `555dde0` | worker_threads 并发 SQLite 写压测（4 场景 × 1000 ops） |
| `packages/daemon/scripts/poc-2-conflict-check.mjs` | `555dde0` | git pre-commit hook 本体（conflict 查询 + fail-open + exit 0） |
| `packages/daemon/scripts/poc-2-hook-bench.mjs` | `555dde0` | hook 端到端延迟 bench（child_process.spawn 模拟 git hook 触发） |

### 代码

今天**没有任何代码改动**。test 数量维持 76（daemon 67 + mcp-server 9）。

---

## 4. 关键决策与对话回放

（编号续接 428 归档决策 19，本次从决策 20 起）

### 决策 20：v2 核心定位 = Agent OS（多 agent 协作内核）

- **内容**：Cairn v2 不是 agent，不是 agent framework，是 agent 之上的基础设施层。类比操作系统：OS 不写文档，管的是文件锁 / 共享内存 / 进程仲裁 / 撤销操作。
- **触发**：用户明示"类似 Windows 系统"这个类比；"协作管理者"这一措辞在对话中出现三次。
- **落地**：PRODUCT.md §0（TL;DR）/ §1.1（一句话定义）/ §2.1（核心命题）。

### 决策 21-24（三动词 / 四能力 / 双通道 / 红线）

以下四条在同一段对话链中连续锁定：

- **决策 21**：三动词 = Dispatch / Rewind / Arbitrate（覆盖"派单 / 回滚 / 仲裁"语义场）
- **决策 22**：四能力 = 冲突可见 / 状态可逆 / 需求可派 / 消息可达（PRODUCT.md §5 全章骨架）
- **决策 23**：用户接 Cairn 两条通道——Inspector（只读 NL 查询，§9.2 路径 A）+ 直接命令（rewind / list / delete，走原 8 工具）
- **决策 24**：红线策略 = B 引导 + C 派单结合；Cairn 自己不修代码（PRODUCT.md §1.3 反定义第 1 条）

### 决策 25：4-persona stress-test 作为持久质量门槛

- **内容**：每次 PRODUCT.md 大改后，用 4 个 persona（核心 ICP / 次要 ICP / anti-ICP / 边缘）各出 5 个硬问题做 stress-test，subagent 模拟作答，暴露的 gap 按 P0/P1/P2 分级处理。
- **触发**：本次 stress-test 暴露 12 条 gap（其中 5 条 P0 当天落地），证明方法有效。
- **落地**：今天跑了一次完整示范，后续作为制度执行。

### 决策 26：pre-impl-validation = v0.1 编码前硬验证闸

- **内容**：4 PoC（race window / git hook / LLM 选型 / subagent 调用率）+ 4 调研（D-1~D-4）全部完成，才进入 W4 编码阶段。
- **触发**：双轨并行产出 ARCHITECTURE.md + pre-impl-validation，两份文档 cross-ref 锚点一致。
- **落地**：`docs/superpowers/plans/2026-04-29-pre-impl-validation.md`。

### 决策 27：SQLite WAL baseline 在 v0.1 ICP 内足够

- **内容**：N≤10 并发 writer 下 success 100% / p99 < 6ms / 零 SQLITE_BUSY 暴露；conflict-detection 应用层可叠加，不需要额外并发原语。
- **触发**：PoC-1 实测数据（`2026-04-29-poc-1-results.md`）。
- **落地**：ARCHITECTURE.md §6.1 锚点回填。

### 决策 28：git hook commit-after 路径 v0.1 完全可用

- **内容**：5/5 场景全 PASS；Node 冷启动 ~70ms 是主要成本；SQLite per-query ~45μs；fail-open 验证完整。Native 二进制优化记为 v0.2/v0.3 候选。
- **触发**：PoC-2 实测数据（`2026-04-29-poc-2-results.md`）。
- **落地**：ARCHITECTURE.md §6.1 + 附录锚点回填。

### 决策 29-30：Floating Marker 形态 + Tauri 技术栈

- **决策 29**：Floating Marker = v0.2 主 UX 形态（路标隐喻，右下角，静物）；取代 v1 桌面宠物 + 取代 v2 早期§8 状态栏图标候选。
- **决策 30**：ADR-8 = Tauri（Rust + WebView，~5-10MB，启动 < 200ms）；Electron 记为备选（如 Rust 采用卡住）。
- **落地**：PRODUCT.md §8 重构（8.1/8.2/8.3）+ §1.3 反定义第 7 条 + ARCHITECTURE.md ADR-8。

### 决策 31：W3 = 前置验证收尾周，不进编码

- **内容**：W3（2026-05-04 起）的目标是把 pre-impl-validation 中只有 dogfood 才能拿到的数据收齐（PoC-4 / D-1 / D-2 / D-4），并起草 W4-W7 编码 plan；W4 才是编码启动周。
- **触发**：用户指令"起 W3 plan"后，发现现有文件是 v1 路线，整体推翻重写。
- **落地**：`docs/superpowers/plans/2026-05-04-wedge-w3.md`（469 行）。

### 决策 32-33：ADR-4 重写（LLM provider-agnostic）+ PoC-3 双 provider

- **决策 32**：ADR-4 从"Sonnet vs local 7B"改为 OpenAI-compatible provider-agnostic 接口（用户配 provider / endpoint / API key / model）。根本理由：项目维护方用 MiniMax / DeepSeek，锁定 Anthropic 与产品调性矛盾。
- **决策 33**：PoC-3 升级为双 provider 验证（推荐 MiniMax + DeepSeek），时间盒 60-120 分钟，同时验证接口可移植性 + 推荐 provider 清单。
- **触发**：用户说"我用 MiniMax 的 coding plan，因为做测试比较便宜"——这是关键语境校正，不是偶然提及。
- **落地**：`2320c03`（ARCHITECTURE.md ADR-4 + poc-3-prep.md 双 provider 版）。

### 决策 34：commit 粒度规则（按逻辑工作单元）

- **内容**：今天曾出现 6 个小 commit；用户要求 squash。规则："按逻辑工作单元，不按完成一个文件"。6 → 3 今天已执行（含 force push）。
- **落地**：本次 squash 完成，后续遵守。backup branch `backup/pre-squash-2026-04-29` 保留在本地。

---

## 5. 数字增量

| 指标 | 4-28 EOD（428 归档值） | 本次归档值 | 增量 |
|---|---|---|---|
| HEAD | `d44c44e` | `2320c03` | +5 commits |
| daemon tests | 90 | 67* | —（注） |
| mcp-server tests | 42 | 9* | —（注） |
| 总 tests | 132 | 76 | —（注） |
| PRODUCT.md 行数 | 617（v1） | 1319（v2） | +702 |
| ARCHITECTURE.md 行数 | 0 | 705 | +705 |
| README.md 行数 | 283 | 339 | +56 |
| plan 文件数 | 8 | 12 | +4（预验证 + PoC-1/2 结果 + PoC-3 prep + PoC-4 tracking） |
| PoC 脚本数 | 0 | 3 | +3 |
| 🚧 锚点已回填 | 0 | 2（PoC-1 ✓ / PoC-2 ✓） | +2 |
| 🚧 锚点待回填 | 8 | 6（PoC-3 / PoC-4 / D-1 / D-2 / D-3 / D-4） | -2 |

> *注：tests 数字在 428 归档中记录为 daemon 90 / mcp-server 42 / 总 132（含 W1-W2 所有 feature 测试）。CLAUDE.md 现在写的是 67 + 9 = 76，可能是因为 W2 feature 测试未统计进 daemon 包，或归档有误差。今天没有改任何测试，实际 test 数量不变。

---

## 6. 8 个 🚧 PoC-D 锚点状态

| 锚点 | 状态 | 说明 |
|---|---|---|
| ✓ PoC-1（race window） | **已验证** | SQLite WAL N≤10 PASS；N=50 v0.3 议题 |
| ✓ PoC-2（git hook 延迟） | **已验证** | 5/5 PASS，fail-open 完整 |
| 🚧 PoC-3（LLM provider-agnostic） | **user 跑**（W3 可选，不阻塞主线） | 已有双 provider 版 prompt deck + rubric |
| 🚧 PoC-4（subagent 调用率） | **user 跑**（W3 主线 Day 1-4） | 已有 tracking template + system prompt 模板 |
| 🚧 D-1（Cursor / Cline / Aider 接入） | **W3 Day 2 调研**（0.5 天时间盒） | 产出 `2026-05-08-d1-d2-research.md` §D-1 |
| 🚧 D-2（daemon 资源 baseline） | **W3 Day 1 启动 + Day 4 收尾** | Idle < 50MB / Busy < 200MB 目标基线 |
| 🚧 D-3（provider 间质量差距） | **PoC-3 一并产出** | 5 维度打分跨 provider 对比 |
| 🚧 D-4（假阳性率） | **dogfood 期间嵌入** | 写入 PoC-4 结果报告附录 |

---

## 7. 当前 git 状态

```
2320c03  docs(spec): ADR-4 refactor — LLM provider-agnostic + PoC-3 prep dual-engine
138e43e  docs(poc): PoC-3 NL prompt deck + PoC-4 dogfood tracking template
ffd3e8b  docs(spec): Floating Marker (v0.2 main UX) + ADR-8 + W3 plan + README v2
555dde0  test(poc): pre-impl validation — PoC-1 race window + PoC-2 git hook
5d8046f  docs(spec): PRODUCT.md v2 (Agent OS pivot) + ARCHITECTURE + validation plan
d44c44e  (W2 EOD baseline，上周 scratchpad.delete)
```

未 committed 工作树：
- `428归档.md`（昨晚归档，未 commit）
- `429归档.md`（本文件，写完后未 commit）

两份归档文件应一起 commit：`docs(archive): 428 + 429 session archive`

---

## 8. 接下来（W3 启动清单）

**用户即将清空上下文，开 PoC-4 dogfood。**

**W3 Day 1（2026-05-04）立刻要做**：

1. 确认 `.mcp.json` 配置（`cairn.scratchpad.list` 冒烟通过）
2. 选 5 个真实开发任务（从 W3 plan §7.1 推荐任务清单 A-E 中选，或替换成自己真实工作）
3. 在每个任务 prompt 顶部加入 PoC-4 协议（见 `poc-4-tracking.md` §1.2 system prompt 模板，英文版，paste-ready）
4. D-2 daemon 资源采样脚本启动（后台跑，主线继续）

**PoC-3 用户自跑（W3 可选，不阻塞主线）**：

需要的两件事：
- MiniMax API key（或 DeepSeek API key）
- 打开 `docs/superpowers/plans/2026-04-29-poc-3-prep.md`，按 §2 step-by-step 跑，每个 prompt 按 5 维度打分，填入双 provider 表格

PoC-3 完成后随时回填 ARCHITECTURE.md ADR-4。

**W3 Day 5（2026-05-08）**：

- PoC-4 结果报告产出（`2026-05-08-poc-4-results.md`）
- D-1 / D-2 调研报告定稿
- W4-W7 编码 plan 草稿就位（`2026-05-11-w4-conflict-v1.md`）
- W3 exit review（`docs/w3-exit-review.md`）
- 三件齐 → W4 编码启动

---

## 9. 风险 + 未决

- **LICENSE**：README 占位 TBD，作者尚未决定开源协议。
- **PoC-4 < 70% 场景**：W3 plan §5 有失败退路（Path B Task tool wrapper 前置 + ADR-9），但路径 b 整体会把 W4 编码推迟 1-2 天，需要心理准备。
- **PoC-4 任务 2（Tauri skeleton）**：依赖 Rust 工具链，未确认用户机器是否已装（`cargo --version`）。如没装，Day 1 setup 时可用任务 A 替换。
- **W3 Day 1 SQLite + Windows 中文路径**：D-2 测量期间可能触发路径编码 edge case；已在 PoC-4 tracking §1.2 给绕法（指定英文路径的 `--db`）。
- **tests 数字对齐**：428 归档记载 132 tests，CLAUDE.md 记载 76 tests，存在出入。下次会话 W3 启动前建议跑一次 `npm test` 确认实际数字。

---

## 10. 反思

**转向速度 vs. 文档完整性**：今天一天完成了 v1 → v2 整个定位转向，13 个新决策，9 份文档新建或全量重写。这个速度是 subagent rotation 加持的——单个 sonnet 不可能在一天内完成这个量。但代价是每个文档都"刚写完"，还没有经过用户实际使用中的摩擦检验。

**4-persona stress-test 有效**：这是今天最有价值的方法论产出。12 条 gap 中 5 条 P0 当天落地，Jess 的 Stripe v3/v2 案例和张涛的 `shared/types.ts` 案例让 §5.2 / §5.4 从抽象变具体。后续作为制度坚持。

**"按语境校正"的成本**：用户一句"我用 MiniMax 的 coding plan"触发了 ADR-4 整体重写。这说明产品定位的假设（用谁的 LLM）和技术设计之间的传导路径要短——产品假设变了，技术决策应该当天跟上，不要等到实施时才发现。今天做到了。

**PoC 比预算快 10 倍**：PoC-1 + PoC-2 预算 1.5 天，实际 1.5 小时。这说明 PoC 设计本身的清晰度够（pre-impl-validation 文档的价值在这里体现），以及 daemon 已有 API 直接可用（不需要搭环境）。PoC-3 / PoC-4 如果同样顺利，W3 时间盒有很大富余，可以把 W4 编码 plan 写得更细。

**force push 是今天唯一的破坏性操作**：commit squash 后 force push 是不得不用的操作。已建 backup branch 保护，但这个操作本身提示：下次应在 day 结束前先 squash 再 push，而不是在已 push 历史上 squash。

---

## 11. 索引

### 今日新增文档

- [`PRODUCT.md`](PRODUCT.md) — v2 全量重写（1319 行，v1 降附录，`5d8046f` + `ffd3e8b` 两次大改）
- [`ARCHITECTURE.md`](ARCHITECTURE.md) — 从零起草（705 行，`5d8046f`；PoC-1/2 回填 `555dde0`；ADR-8 + ADR-4 更新 `ffd3e8b` + `2320c03`）
- [`README.md`](README.md) — v2 全量重写（339 行，`ffd3e8b`）
- [`docs/superpowers/plans/2026-04-29-pre-impl-validation.md`](docs/superpowers/plans/2026-04-29-pre-impl-validation.md) — 验证闸设计（`5d8046f`）
- [`docs/superpowers/plans/2026-04-29-poc-1-results.md`](docs/superpowers/plans/2026-04-29-poc-1-results.md) — PoC-1 结果报告（201 行，`555dde0`）
- [`docs/superpowers/plans/2026-04-29-poc-2-results.md`](docs/superpowers/plans/2026-04-29-poc-2-results.md) — PoC-2 结果报告（188 行，`555dde0`）
- [`docs/superpowers/plans/2026-05-04-wedge-w3.md`](docs/superpowers/plans/2026-05-04-wedge-w3.md) — W3 plan v2（469 行，`ffd3e8b`）
- [`docs/superpowers/plans/2026-04-29-poc-3-prep.md`](docs/superpowers/plans/2026-04-29-poc-3-prep.md) — PoC-3 双 provider prompt deck（561 行，`138e43e`）
- [`docs/superpowers/plans/2026-05-04-poc-4-tracking.md`](docs/superpowers/plans/2026-05-04-poc-4-tracking.md) — PoC-4 dogfood tracking（550 行，`138e43e`）

### 今日新增脚本

- [`packages/daemon/scripts/poc-1-race-stress.mjs`](packages/daemon/scripts/poc-1-race-stress.mjs) — worker_threads 并发 SQLite 写压测
- [`packages/daemon/scripts/poc-2-conflict-check.mjs`](packages/daemon/scripts/poc-2-conflict-check.mjs) — git pre-commit hook 本体
- [`packages/daemon/scripts/poc-2-hook-bench.mjs`](packages/daemon/scripts/poc-2-hook-bench.mjs) — hook 延迟 bench harness

### 今日 Commits

| SHA | message | 主要内容 |
|---|---|---|
| `5d8046f` | `docs(spec): PRODUCT.md v2 (Agent OS pivot) + ARCHITECTURE + validation plan` | PRODUCT.md v2 + ARCHITECTURE.md 初稿 + pre-impl-validation |
| `555dde0` | `test(poc): pre-impl validation — PoC-1 race window + PoC-2 git hook` | 3 个 PoC 脚本 + 两份结果报告 + ARCHITECTURE 锚点回填 |
| `ffd3e8b` | `docs(spec): Floating Marker (v0.2 main UX) + ADR-8 + W3 plan + README v2` | Floating Marker + ADR-8 + W3 plan + README 全量重写 |
| `138e43e` | `docs(poc): PoC-3 NL prompt deck + PoC-4 dogfood tracking template` | PoC-3 prep（双 provider）+ PoC-4 tracking 模板 |
| `2320c03` | `docs(spec): ADR-4 refactor — LLM provider-agnostic + PoC-3 prep dual-engine` | ADR-4 重写 + PoC-3 双 provider 版更新 |

### 归档链

- [`426归档.md`](426归档.md) — P1 + W1 楔 MVP，32 commits
- [`427归档.md`](427归档.md) — W2 Day 1 全口径，17 commits，91 tests
- [`428归档.md`](428归档.md) — W2 周二 + 周三早间，12 commits，132 tests（决策 12-19）
- [`429归档.md`](429归档.md) — 本文件（v2 转向全日，5 commits，决策 20-34）
