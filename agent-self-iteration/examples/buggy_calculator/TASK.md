Fix every failing test in `tests/test_calc.py`.

Constraints:
- Do not modify the tests. They are the spec.
- Keep functions in `src/calc.py` simple — no new dependencies.
- After fixing, all tests must pass under `pytest -q`.
