# Cairn v0.1 · W3 计划（前置验证收尾周 — 进入四能力 v1 编码前最后一道闸）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal**：W3 是**前置验证收尾周**，不是决策周、不是编码周。目标是把 pre-impl-validation 中剩余的、只有 dogfood 才能拿到的数据收齐（PoC-4 / D-1 / D-2 / D-4），同时起草 W4-W7 四能力 v1 编码计划，让 W4 能直接进入编码阶段而不需要补验证。

**Architecture**：ARCHITECTURE.md 骨架（705 行，8 个 🚧 锚点，其中 PoC-1 / PoC-2 已回填）已就位。W3 的工作是填 PoC-4 / D-1 / D-2 三个剩余锚点，并为 W4-W7 起草 plan。不写新工具，不改 schema，不进编码阶段。

**Tech Stack**：Node.js 24 · TypeScript strict ESM · `better-sqlite3@^12.9.0` · `@modelcontextprotocol/sdk@^1`（同 W1/W2，W3 本身不写代码）

**Spec 来源**：
- `PRODUCT.md` v2 §0 TL;DR / §3 ICP / §5 四能力 / §6 功能范围 / §9.2 三路径 / §10.2 路线图
- `ARCHITECTURE.md` §6 四能力实施路径 / §7 ADR-1~ADR-8 / 附：🚧 锚点索引
- `docs/superpowers/plans/2026-04-29-pre-impl-validation.md` §3 PoC 详细 plan / §4 调研项 / §5 优先级
- `docs/superpowers/plans/2026-04-29-poc-1-results.md`（PoC-1 PASS，已回填）
- `docs/superpowers/plans/2026-04-29-poc-2-results.md`（PoC-2 PASS，已回填）

**配套技能**（执行时按 task 类型选用）：
- `superpowers:subagent-driven-development` — 调研 / dogfood 任务（每个独立 task 派 fresh sonnet，避免上下文积累）
- `superpowers:verification-before-completion` — 每天 EOD 验收 + W3 末 W4 启动判据核对
- `superpowers:writing-plans` — Task 3.5.3 起草 W4-W7 plan 草稿

---

## 1. W3 不变量（硬约束）

以下约束在本周任何 task 执行过程中不得破坏。遇到模糊情况优先拒绝，不要自行解释越界。

- **不写新 MCP 工具**（保持 8 工具契约不变；新工具规划在 ARCHITECTURE.md §5.2，W4+ 实施）
- **不改 schema**（migration 001-003 已锁定；checksum guard 会拒绝已落地 migration 的任何修改）
- **不进 v0.1 W4-W7 编码阶段**（W3 是验证收尾，W4 才是编码启动）
- **不重新审视 v2 定位**（v2 已在 4-29 锁定，W3 围绕 v2 + ARCHITECTURE.md 干活，不讨论路线转向）
- **不修改 pre-impl-validation.md**（它是 W3 的输入文档，不是 W3 的产物）
- **不修改 PRODUCT.md / ARCHITECTURE.md 正文**（只允许回填 🚧 锚点，锚点外内容不动）
- **W3 末必须有书面 W4 启动判据**：PoC-4 报告 + D-1/D-2 结论 + W4-W7 plan 草稿三件齐才算达标
- **PoC-4 数据如果回来 < 70% 调用率**（路径 a 不可靠）→ W3 末立即启动 Path B Task tool wrapper 设计（pre-impl-validation §3.4 失败应对），记为 ADR-9 候选

---

## 2. W3 战略目标

| 目标 | 类型 | 时间盒 | 产出 |
|---|---|---|---|
| **PoC-4 dogfood 启动与完成** | dogfood 实测 | 贯穿 Day 1-4 | `docs/superpowers/plans/2026-05-08-poc-4-results.md` |
| **D-1 Cursor/Cline 接入路径调研** | 调研 | 0.5 天（Day 2） | `docs/superpowers/plans/2026-05-08-d1-d2-research.md` §D-1 段 |
| **D-2 daemon 资源占用 baseline** | 测量 | 0.5 天（Day 1 启动，Day 4 收尾） | `docs/superpowers/plans/2026-05-08-d1-d2-research.md` §D-2 段 |
| **D-4 假阳性率收集** | dogfood 数据 | 贯穿全周（嵌入 PoC-4） | 写入 PoC-4 结果报告附录 |
| **W4-W7 编码 plan 起草** | 规划 | Day 3-5 | `docs/superpowers/plans/2026-05-11-w4-conflict-v1.md`（草稿） |
| **ARCHITECTURE.md 锚点回填** | 文档 | Day 5 | PoC-4 / D-1 / D-2 三处 🚧 锚点 → ✓ |

**PoC-3 状态**：user 自己跑（LLM 选型对比），跑完后回填 ARCHITECTURE.md ADR-4。这是 W3 期间随时可发生的事件，不是 W3 强制任务，W3 不为其排期。

---

## 3. Day-by-day 任务分解

---

### 3.1 Day 1 · 周一 (2026-05-04) — D-2 启动 + PoC-4 准备

#### Task 3.1.1：D-2 daemon 资源 baseline 测量（后台跑）

**目标**：测 idle / busy 两种状态下 daemon 的 RAM / CPU，给 ARCHITECTURE.md §8.1 回填数据。

参考 `pre-impl-validation.md` §4.2 的完整步骤。

- [ ] **Step 1**：启动 mcp-server（或 daemon），等待 5 分钟稳定（Windows 11 上 Node 进程稳定后采样更准）
- [ ] **Step 2**：Idle 测量：1 小时内无 MCP 调用，每 10 秒采样一次 RSS / CPU%（用 `process.memoryUsage()` + Windows `tasklist /FI "PID eq <pid>"` 或 Node `--inspect` + `clinic`）
- [ ] **Step 3**：Busy 测量：用 PoC-1 的压测脚本维持 5 calls/sec（`scratchpad.write` + `checkpoint.create` 混合），持续 15 分钟，同样采样
- [ ] **Step 4**：记录 p50 / p99 / max RSS 和 CPU%；写入 `docs/d2-raw-data.md`（草稿，Day 4 整理进正式报告）
- [ ] **Step 5**：如果 Idle > 100MB 或 Busy > 400MB，立即标注 🔴 超标，当天在 `docs/d2-raw-data.md` 记录 hot path 猜测（不等 Day 4 才处理）

**时间盒**：0.5 天，可后台跑（采样脚本挂着，主线程做 Task 3.1.2）

#### Task 3.1.2：PoC-4 dogfood 准备（user 接管）

**目标**：user 装好 `.mcp.json`，选定 5 个真实开发任务，配置好 system prompt 模板，让 PoC-4 从 Day 2 起能实际跑。

- [ ] **Step 1**：确认 `.mcp.json` 已配置（指向本地 `packages/mcp-server`，`node dist/index.js` 启动方式）；用 `cairn.scratchpad.list` 冒烟测试工具可用
- [ ] **Step 2**：从 cairn 项目 backlog 选 5 个真实开发任务（每个任务要能 spawn 3+ subagent）。建议候选：
  - 补 daemon 仓储层测试覆盖（CheckpointRepo / LaneRepo / OpsRepo 的边界场景）
  - 写 git hook 安装脚本原型（ARCHITECTURE.md §6.1 提到的 `cairn install` 命令）
  - 整理 PRODUCT.md 残余 v1 痕迹和 §17 MCP 契约占位段
  - 起草 `packages/conflict-engine/` 的目录骨架和 README 占位（ARCHITECTURE.md §3.3 候选包）
  - 起草 W4-W5 冲突可见 v1 的 migration 004 schema 草案（仅文档，不 commit DDL）
- [ ] **Step 3**：在每个任务的启动 prompt 里加入 PoC-4 约定（参考 `pre-impl-validation.md` §3.4 Step 1 的模板）：

  ```
  每个被派出的 subagent 在任务结束前必须调用 cairn.scratchpad.write，
  key 格式为 subagent/{agent_id}/result，
  内容为完整报告（包含：完成内容摘要、关键决策、未解决问题、建议后续步骤）。
  不调用此工具视为任务未完成。
  ```

- [ ] **Step 4**：建立 `docs/poc-4-tracking.md`，列 5 个任务 + 每个任务的 subagent 调用记录表（初始空表）；commit：`docs(poc-4): tracking sheet + dogfood task list`

**输出**：`docs/poc-4-tracking.md`（持续更新）；`.mcp.json` 验证可用

#### Task 3.1.3：楔级技术债清理范围确认

**目标**：参考 W2 EOD 状态（`427归档.md` 或等效归档），确认 W3 期间是否有 1-2 项不动 schema 的可清理技术债。如果有，列进 Day 1 EOD 的待办。

- [ ] **Step 1**：查看 `ARCHITECTURE.md §9 已知技术债与风险` 表格，过滤出"清理时机 = W3 dogfood 启动时"的项（如 Windows 路径编码问题、stash SHA label 污染）
- [ ] **Step 2**：对每项判断：是否真的可以在不动 schema 的前提下修？如果是，加进 Day 3 待办；如果不是，在此处标注推迟理由即可
- [ ] **Step 3**：把范围确认结果（列表 + 每项的可否）追加到 `docs/poc-4-tracking.md` 末尾的"技术债附录"段

**EOD Mon 验收**：
- [ ] D-2 采样脚本已跑起来（后台），`docs/d2-raw-data.md` 有第一批数据
- [ ] `.mcp.json` 验证可用，`cairn.scratchpad.list` 冒烟通过
- [ ] `docs/poc-4-tracking.md` 存在，5 个任务已列，system prompt 模板已准备
- [ ] 技术债清理范围已确认（0 项 / 1-2 项，结论写下来即可）

---

### 3.2 Day 2 · 周二 (2026-05-05) — PoC-4 跑第 1-2 个任务 + D-1 调研

#### Task 3.2.1：PoC-4 任务 #1 + #2（user 接管）

**目标**：跑前两个真实开发任务，每个 spawn 3+ subagent；记录 subagent 调用 `cairn.scratchpad.write` 的情况。

- [ ] **Step 1**：用 Claude Code Task tool 跑任务 #1（按 Day 1 准备的任务列表），spawn ≥3 subagent
- [ ] **Step 2**：每个 subagent 结束后，立即用 `cairn.scratchpad.list` 检查对应 key 是否存在；记录到 `docs/poc-4-tracking.md`（调用 / 未调用 / 调用但内容空洞 < 50 字）
- [ ] **Step 3**：重复 Step 1-2 跑任务 #2
- [ ] **Step 4**：记录当天 D-4 数据——是否出现"若冲突检测已实现，会报什么"的场景；写入 `docs/poc-4-tracking.md` 的"D-4 假阳性观测"列

#### Task 3.2.2：D-1 调研 Cursor / Cline / Aider 接入路径

**目标**：查清楚 Cursor / Cline / Aider 是否有 MCP-aware 接入路径，给 ARCHITECTURE.md ADR-6 提供数据。参考 `pre-impl-validation.md` §4.1 的完整手段。

- [ ] **Step 1**：查 Cursor 官方文档（MCP 支持现状 / Extension API）；检查是否有 `.mcp.json` 等效配置入口
- [ ] **Step 2**：查 Cline GitHub README + docs（是否支持 MCP server 接入）
- [ ] **Step 3**：查 Aider 文档（是否有 MCP plugin 支持 / tool-call API hook）
- [ ] **Step 4**：填写 `docs/superpowers/plans/2026-05-08-d1-d2-research.md`（新建）的 §D-1 段，格式：

  | Host | 是否 MCP-aware | 接入方式 | 工作量估算 | v0.1 建议 |
  |---|---|---|---|---|
  | Claude Code | 是 | 原生 `.mcp.json` | 已完成 | 主要 ICP |
  | Cursor | 待查 | ... | ... | ... |
  | Cline | 待查 | ... | ... | ... |
  | Aider | 待查 | ... | ... | ... |

- [ ] **Step 5**：D-3 接入成本对比顺手做（`pre-impl-validation.md` §4.3）：与 CC 接入工作量对比，写一句结论（"Cursor 有原生 MCP 支持，接入成本 0.5 天"或"Cline 无路径，推 v0.2"）
- [ ] **Step 6**：commit 初版 `docs/superpowers/plans/2026-05-08-d1-d2-research.md`（§D-1 填完）

**时间盒**：0.5 天

#### Task 3.2.3：D-2 后续采样记录

- [ ] 检查 D-2 采样数据（`docs/d2-raw-data.md`），确认后台采样正常；如有异常（Node 进程已死 / 数据断档），重启并记录原因
- [ ] 如 Busy 测量还没跑，今天补跑压测 5 calls/sec 场景

**EOD Tue 验收**：
- [ ] PoC-4 任务 #1 + #2 完成，`docs/poc-4-tracking.md` 有至少 6 个 subagent 样本记录
- [ ] `docs/superpowers/plans/2026-05-08-d1-d2-research.md` §D-1 段已填（含 Cursor / Cline / Aider 三行）
- [ ] D-2 采样数据有至少 2 小时的 idle 记录 + 一段 busy 记录

---

### 3.3 Day 3 · 周三 (2026-05-06) — PoC-4 跑第 3-4 个任务 + W4 plan 起草启动

#### Task 3.3.1：PoC-4 任务 #3 + #4

同 Task 3.2.1 模式：

- [ ] 跑任务 #3（spawn ≥3 subagent），记录调用数据
- [ ] 跑任务 #4（spawn ≥3 subagent），记录调用数据
- [ ] 更新 `docs/poc-4-tracking.md`；当前样本数应累计 ≥12
- [ ] 继续记录 D-4 假阳性观测数据

#### Task 3.3.2：技术债可选 fix（如 Day 1 确认有可修项）

**仅当 Day 1 Task 3.1.3 确认有不动 schema 的可修项时执行**：

- [ ] 取第 1 项技术债（不改 schema、不改已有接口），先写 failing test，确认 RED
- [ ] 最小改动实现修补；跑 `cd packages/<pkg> && npm test && npx tsc --noEmit`，确认 GREEN
- [ ] commit：`fix(<module>): <描述>` 或 `chore(<module>): <描述>`

如果 Day 1 确认 0 项可修，本 task 跳过。

#### Task 3.3.3：起草 W4-W5 冲突可见 v1 plan

**目标**：基于 ARCHITECTURE.md §6.1 + PRODUCT.md §10.2 第一行（"W3-W5 冲突检测 v1"），起草 W4 plan 的骨架。

- [ ] **Step 1**：读 ARCHITECTURE.md §6.1 冲突可见的完整数据流（MCP-call 级 + commit-after 级双层），摘出 W4-W5 需要实现的 task 列表：
  - migration 004：`processes` 表 + `conflicts` 表 DDL（见 ARCHITECTURE.md §4.3）
  - `cairn.process.register` / `heartbeat` / `list` / `status` 4 个新工具
  - daemon in-flight 路径比对逻辑（冲突检测应用层）
  - git pre-commit hook 安装脚本（`cairn install` 命令）
  - `cairn.conflict.list([since])` 工具
  - `cairn.inspector.query(nl_query)` 工具（基础版，纯 SQL 确定性查询，不走 LLM）
- [ ] **Step 2**：在 `docs/superpowers/plans/2026-05-11-w4-conflict-v1.md`（新建）建文件骨架：顶部 metadata（标题 / Goal / Architecture / Tech Stack / Spec 来源）+ 不变量段（留空待 Day 5 填充）+ Day-by-day 结构（Day 1-5 各一节，先只写标题）
- [ ] **Step 3**：把 Step 1 的 task 列表分配到对应 Day（初步，Day 5 细化）
- [ ] **Step 4**：commit 骨架版：`docs(plan): W4-W5 conflict-v1 plan skeleton`

**EOD Wed 验收**：
- [ ] PoC-4 累计 ≥12 个 subagent 样本，`docs/poc-4-tracking.md` 数据实时
- [ ] `docs/superpowers/plans/2026-05-11-w4-conflict-v1.md` 骨架版已存在（未完成，但结构在）
- [ ] 如有技术债 fix，已 commit 且全套测试绿

---

### 3.4 Day 4 · 周四 (2026-05-07) — PoC-4 收尾 + 调研结论书面化

#### Task 3.4.1：PoC-4 任务 #5（最后一个）

- [ ] 跑任务 #5（spawn ≥3 subagent）；更新 `docs/poc-4-tracking.md`；总样本数应 ≥15
- [ ] 继续记录 D-4 假阳性观测（最后一轮）

#### Task 3.4.2：PoC-4 数据汇总 + 初步 verdict

**目标**：对 `docs/poc-4-tracking.md` 的原始记录做第一轮统计，得出调用率数字，形成初步 verdict。

- [ ] **Step 1**：统计总 subagent 数 / 完整调用数（content ≥ 50 字）/ 未调用数 / 空洞调用数
- [ ] **Step 2**：计算调用率 = 完整调用数 / 总 subagent 数
- [ ] **Step 3**：对照 `pre-impl-validation.md` §3.4 的决策矩阵：
  - ≥ 90% → 路径 (a) 可行，按原 ARCHITECTURE.md §6.4 设计走
  - 70%-90% → 可行但需兜底机制：主 agent 在读 scratchpad 前先 check 哪些 subagent 没写
  - < 70% → 路径 (a) 不可靠，W3 末立即起 ADR-9 / 路径 (b) Task tool wrapper 设计
- [ ] **Step 4**：把初步 verdict 写入 `docs/poc-4-tracking.md` 末尾的"Day 4 中间 verdict"段
- [ ] **Step 5**：如果调用率 < 70%，**当天就开始**起草 `docs/path-b-wrapper-design.md`（Task tool wrapper 设计草案，不等 Day 5）

#### Task 3.4.3：起草 W5-W7 Dispatch v1 / 消息可达 v1 plan 片段

**目标**：基于 ARCHITECTURE.md §6.3（需求可派）+ §6.4（消息可达），给 W4 plan 文档追加 W5-W7 的范围段。

- [ ] **Step 1**：读 ARCHITECTURE.md §6.3 Dispatch 实现流程，摘出 W5-W7 task 列表：
  - migration 005：`dispatch_requests` 表 DDL
  - Dispatch LLM 调用模块（🚧 PoC-3 锚点）
  - `cairn.dispatch.request` / `cairn.dispatch.confirm` 两个工具
  - scratchpad IPC 约定文档化（`subagent/{agent_id}/result` 命名规范正式化）
- [ ] **Step 2**：读 ARCHITECTURE.md §6.4 消息可达的 prompt 模板建议，确认 W7-W9 的工作边界（v0.1 = 层 1 + 层 2，不做反汇总 L3）
- [ ] **Step 3**：追加到 `docs/superpowers/plans/2026-05-11-w4-conflict-v1.md` 末尾，作为"W5-W7 范围指针"附录段（不需要精细，指针级别即可）
- [ ] **Step 4**：commit：`docs(plan): W4 plan + W5-W7 range pointers`

#### Task 3.4.4：D-1 / D-2 结论书面化

- [ ] **D-1 结论**：把 Day 2 `docs/superpowers/plans/2026-05-08-d1-d2-research.md` §D-1 的表格转化为一句话结论，例："Cursor v0.44+ 有原生 MCP 支持（`.cursor/mcp.json`），接入成本约 0.5 天；Cline 无 MCP 标准路径，推 v0.2；Aider 无路径，不进 ICP。"
- [ ] **D-2 结论**：整理 `docs/d2-raw-data.md` 的原始数据，填写 `docs/superpowers/plans/2026-05-08-d1-d2-research.md` §D-2 段（对照 `pre-impl-validation.md` §4.2 的目标基线 Idle < 50MB / Busy < 200MB）
- [ ] **D-4 结论**：统计 dogfood 期间的冲突假阳性观测数据（观察到的"若检测已实现会报什么"），写入 D1-D2 报告附录或 PoC-4 报告附录
- [ ] commit：`docs(research): D-1 D-2 D-4 conclusions`

**EOD Thu 验收**：
- [ ] PoC-4 总样本数 ≥15，初步调用率数字已统计
- [ ] `docs/superpowers/plans/2026-05-08-d1-d2-research.md` §D-1 + §D-2 结论段均有内容
- [ ] `docs/superpowers/plans/2026-05-11-w4-conflict-v1.md` 有 W5-W7 范围指针段
- [ ] 如调用率 < 70%，`docs/path-b-wrapper-design.md` 已建立草稿

---

### 3.5 Day 5 · 周五 (2026-05-08) — W3 收尾 + W4 启动判据核对

#### Task 3.5.1：PoC-4 正式结果报告

**目标**：仿 `2026-04-29-poc-1-results.md` / `poc-2-results.md` 的格式，输出 PoC-4 结果报告。

- [ ] **Step 1**：新建 `docs/superpowers/plans/2026-05-08-poc-4-results.md`，结构：
  - § 0 TL;DR（Verdict：PASS / NOTE / FAIL + 30 秒读完的结论）
  - § 1 测试设计（5 个任务 + spawn 规则 + system prompt 模板）
  - § 2 数据（汇总表：任务 / subagent 数 / 调用 / 未调用 / 调用率 / 内容长度分布）
  - § 3 关键发现（调用率数字 + 失败原因分析：任务太短？prompt 被截断？subagent 未读到约定？）
  - § 4 Verdict 细化与决策（对照 pre-impl-validation §3.4 决策矩阵）
  - § 5 ARCHITECTURE.md 🚧 PoC-4 锚点回填文本
  - § 6 D-4 假阳性附录（dogfood 期间观测到的潜在冲突场景 + 评估是否为假阳性）
- [ ] **Step 2**：commit：`docs(poc-4): results report — subagent call-rate dogfood`

#### Task 3.5.2：D-1 / D-2 调研报告定稿

- [ ] 确认 `docs/superpowers/plans/2026-05-08-d1-d2-research.md` 两个段都有结论 + 数据
- [ ] §D-1 末尾加"v0.1 ICP 建议"：如果 Cursor 有原生 MCP 支持，建议加入 v0.1 ICP 支持范围（仅 `.cursor/mcp.json` 配置，不写 wrapper）；如果无路径，维持 ADR-6 原文（CC-only for v0.1）
- [ ] §D-2 末尾加"超标应对"（如有）：如果 Busy 超标，列 hot path + 优化建议（不要求本周实施，Week 4 前处理即可）
- [ ] commit：`docs(research): D-1 D-2 research report final`

#### Task 3.5.3：W4-W7 plan 草稿 review + 锁定

**目标**：把 `docs/superpowers/plans/2026-05-11-w4-conflict-v1.md` 从骨架版补成可执行版。

- [ ] **Step 1**：补完 Day-by-day 任务分解（Day 1-5），每个 task 有：目标 + Step 列表 + EOD 验收
- [ ] **Step 2**：补完顶部 metadata：Goal / Architecture / Tech Stack / Spec 来源 / 配套技能
- [ ] **Step 3**：补 W3 不变量段（W4 不变量：不改已落地 migration 001-003 / 新 migration 004 只加不改 / 测试只增不减）
- [ ] **Step 4**：补 Exit criteria（≤10 条，按重要度排序）
- [ ] **Step 5**：如果 PoC-4 < 70%，在 W4 plan 顶部加"前置依赖"段：W4 Day 1-2 先完成 Path B wrapper 设计（`docs/path-b-wrapper-design.md` 细化），Day 3 才开始 migration 004
- [ ] **Step 6**：commit：`docs(plan): W4-W5 conflict-v1 plan — ready for W4 kickoff`

#### Task 3.5.4：ARCHITECTURE.md 三处锚点回填

**目标**：把 ARCHITECTURE.md 中 PoC-4 / D-1 / D-2 的 🚧 标记更新为 ✓ + 结论摘要。

- [ ] **PoC-4 锚点**（ARCHITECTURE.md §6.4 "兜底机制"段）：将 🚧 替换为 ✓，引用 `2026-05-08-poc-4-results.md` 的结论
- [ ] **D-1 锚点**（ARCHITECTURE.md ADR-6）：将 🚧 替换为 ✓，写一句结论（Cursor 接入路径 / v0.1 ICP 建议）
- [ ] **D-2 锚点**（ARCHITECTURE.md §4.4 + §8.1）：将 🚧 替换为 ✓，写 Idle / Busy 实测数字
- [ ] commit：`docs(arch): backfill PoC-4 D-1 D-2 anchors in ARCHITECTURE.md`

#### Task 3.5.5：W3 EOD review + W4 启动判据核对

**目标**：对照 §4（W3 完成判据）逐条核查，确认 W4 是否可以启动。

- [ ] 新建 `docs/w3-exit-review.md`，内容：
  - W3 完成判据逐条：✅ 达成 / ❌ 未达成 / 🟡 部分达成（每条一句证据，含文件路径）
  - PoC-4 路径选择：路径 (a) 直接走 / 路径 (a) + 兜底机制 / 路径 (b) wrapper 前置
  - W4 启动 gate：是否满足"三件齐"（PoC-4 报告 + D-1/D-2 报告 + W4 plan 草稿）
  - 如有未达成项：写明是 blocking 还是 non-blocking + 应对方案
- [ ] commit：`docs(wedge): W3 exit review + W4 kickoff gate`

#### Task 3.5.6：commit + push（可选）

如果用户希望本周工作推到远端：

- [ ] 确认所有 Task 3.1.1 ~ Task 3.5.5 的 commit 都进 main 分支
- [ ] 按 CLAUDE.md 推送规范（PAT token URL 方式），push 到 `Upp-renlab/Cairn`
- [ ] push 命令：

  ```bash
  TOKEN=$(cat .cairn-push-token/token.txt | tr -d '[:space:]')
  git push "https://x-access-token:${TOKEN}@github.com/Upp-renlab/Cairn.git" main
  # 输出中 TOKEN 替换为 <REDACTED>
  ```

---

## 4. W3 完成判据（W4 启动 gate）

以下条件全部满足时，W3 关闭，W4 编码启动：

- [ ] **PoC-4 报告产出**：至少 15 个 subagent 样本；调用率数字清晰；路径 (a) / (b) 决策写入 ARCHITECTURE.md §6.4 锚点
- [ ] **D-1 报告产出**：Cursor / Cline / Aider 三个 host 的接入路径决策表完整；v0.1 ICP 建议明确（是否扩展到 Cursor）
- [ ] **D-2 报告产出**：Idle / Busy 两种状态的 RSS / CPU% 实测数字；超标时有 hot path 记录
- [ ] **D-4 数据附录**：dogfood 期间的冲突假阳性观测记录（哪怕"0 次冲突场景"也是数据）
- [ ] **ARCHITECTURE.md 三处 🚧 锚点回填**：PoC-4 / D-1 / D-2 三处均更新为 ✓ + 结论
- [ ] **W4-W7 plan 草稿 ready**：`docs/superpowers/plans/2026-05-11-w4-conflict-v1.md` 有可执行的 Day 1-5 分解 + Exit criteria
- [ ] **`docs/w3-exit-review.md` 存在**，W4 启动 gate 明确（是否满足三件齐）
- [ ] **如果 PoC-4 < 70%**：`docs/path-b-wrapper-design.md` 草稿存在，W4 plan 中 Path B wrapper 前置已排期，记为 ADR-9 候选

---

## 5. 失败时退路

| 情景 | 退路方案 | 对 W4 的影响 |
|---|---|---|
| PoC-4 调用率 70-90% | 在 ARCHITECTURE.md §6.4 加兜底机制：主 agent 在读 scratchpad 前先 check 未回报的 subagent 列表；W4 plan 中加对应 task | W4 工期不变，W5 消息可达 v1 多加 1-2 天实现兜底逻辑 |
| PoC-4 调用率 < 70% | 路径 (b) wrapper 前置到 W4；`docs/path-b-wrapper-design.md` 细化；ADR-9 起草；原 W4-W5 冲突可见 v1 推迟一周 | W4 Day 1-2 做 wrapper 设计，Day 3+ 才进冲突可见编码；W5-W7 相应后移 |
| D-1 Cursor 接入完全无路径 | 维持 ADR-6 原文（v0.1 CC-only）；PRODUCT.md §3.1 加一行脚注"v0.1 ICP = CC 用户"；不做额外适配工作 | W4 plan 不需要 Cursor 适配 task，工期缩小 |
| D-2 daemon Idle > 100MB 或 Busy > 400MB | 在 ARCHITECTURE.md §8.1 加 perf 调优 task（W4 优先级 P1）；用 `clinic flame` 找 hot path；如未解决则阻塞 dogfood 扩展 | W4 Day 1 加 perf profiling task |
| PoC-4 跑完 5 个任务但总样本 < 15 | 加跑第 6 个任务（当天或 Day 5 前）；15 是最小样本数，低于此数据可信度不足，不能基于它做架构决策 | 可能 W3 推迟 1 天，但不阻塞 W4 方向决策 |
| W4-W7 plan 草稿 Day 5 仍未完成 | 写精简版（只有 W4 plan，W5-W7 作为"范围指针"附录）；W4 末补完 W5-W7 detail；不因此推迟 W4 启动 | W4 编码启动不依赖 W5-W7 plan 的完整性 |

---

## 6. 与上下游 plan 的关系

**上游（W3 的输入）**：
- W1+W2：8 个 MCP 工具全部落地，scratchpad CRUD + checkpoint + rewind + task_id + auto-checkpoint 已就位
- 4-29 v2 转向：PRODUCT.md v2 + ARCHITECTURE.md + pre-impl-validation + PoC-1 PASS + PoC-2 PASS
- Floating Marker 设计已落（v0.2 主形态，Tauri，ADR-8）

**下游（W3 的产出供给）**：
- W4 plan：`docs/superpowers/plans/2026-05-11-w4-conflict-v1.md`（W3 末产出）
- W5-W7 范围指针：附在 W4 plan 末尾，W4 末细化
- impl-kickoff 时机：W4 Mon 直接开始编码（W3 完成判据满足的前提下）

**PoC-3 状态**（不是 W3 强制任务）：user 自己跑 Dispatch LLM 对比实验（ARCHITECTURE.md ADR-4 锚点）。跑完后随时回填 ARCHITECTURE.md ADR-4；如果 W3 期间完成，W4 plan 可以明确 LLM 选型；如果 W4 前未完成，W4 plan 用默认值（Sonnet API）+ 🚧 PoC-3 标注。

---

## 7. 附：PoC-4 实施细节

### 7.1 推荐任务清单（user 自己挑 5 个）

以下是从 cairn 项目本身 backlog 提取的候选任务，每个都能合理 spawn 3+ subagent：

| # | 任务描述 | 推荐 subagent 拆法 |
|---|---|---|
| A | 补 daemon 仓储层测试覆盖（`CheckpointRepo` / `LaneRepo` / `OpsRepo` 边界场景） | subagent-1: Checkpoint 边界测试；subagent-2: Lane 状态机测试；subagent-3: Ops + Compensations 测试 |
| B | 整理 ARCHITECTURE.md §9 技术债表格（调研每项的清理时机是否仍准确，写更新建议） | subagent-1: 前 5 条；subagent-2: 后 5 条；subagent-3: Windows 路径风险专项 |
| C | 起草 migration 004 schema 设计文档（`processes` + `conflicts` + `dispatch_requests` DDL 审查，只写文档，不 commit DDL） | subagent-1: processes 表设计 review；subagent-2: conflicts 表 review；subagent-3: dispatch_requests 表 review |
| D | 起草 git hook 安装脚本接口设计（`cairn install` 命令 + hook 模板）文档 | subagent-1: 命令行接口设计；subagent-2: hook 模板内容；subagent-3: Windows / macOS / Linux 差异分析 |
| E | 起草 `cairn.process.register` / `heartbeat` / `list` 三个工具的接口规格文档（输入输出 / 错误码 / 并发语义） | subagent-1: register 规格；subagent-2: heartbeat + list 规格；subagent-3: 并发语义 + DEAD 状态 GC 规格 |

### 7.2 system prompt 模板（完整版）

在每个任务的主 agent prompt 顶部或 system prompt 里加入：

```
# Cairn subagent 协议

这个任务使用 Cairn 进行多 subagent 协作。当你（或你派出的 subagent）完成任务时，
在退出前必须执行以下调用：

cairn.scratchpad.write(
  key: "subagent/<你的 agent_id>/result",
  content: <完整报告，必须包含：
    1. 完成内容摘要（2-5 句）
    2. 关键决策列表（逐条）
    3. 未解决问题（如有）
    4. 建议后续步骤（如有）
    最小长度：50 字>,
  task_id: "<当前 task_id>"
)

不调用此工具视为任务未完成。
```

### 7.3 数据记录表格（`docs/poc-4-tracking.md` 格式）

```markdown
## 任务记录

| 任务 | subagent_id | 调用? | 内容长度(字) | 内容质量 | 未调用原因猜测 |
|---|---|---|---|---|---|
| 任务A | SA-001 | ✅ | 280 | 含决策 | — |
| 任务A | SA-002 | ❌ | — | — | prompt 被截断? |
| ...  | ...     | ... | ... | ... | ... |

## 汇总（Day 4 更新）

- 总样本数：
- 完整调用（≥50字）：
- 未调用：
- 空洞调用（<50字）：
- **调用率**：

## D-4 假阳性观测

| 时间 | 任务 | 观测场景描述 | 若检测已实现会报? | 是否真冲突 |
|---|---|---|---|---|
| ... | ... | ... | 是/否 | ... |
```

---

## W3 不做的事（防止任务蔓延）

- 不写第 9 个 MCP 工具（保持 8 工具契约）
- 不改 migration 001-003 的 DDL（migration 是 append-only）
- 不实现 migration 004 代码（只写文档 / 草案，不 commit DDL 或仓储层实现）
- 不实现冲突检测逻辑（W4 任务）
- 不实现进程总线（W4 任务）
- 不实现 Dispatch（W5-W7 任务）
- 不实现悬浮标 / Tauri（v0.2 任务）
- 不修改 PRODUCT.md 正文（只允许锚点回填）
- 不重写 pre-impl-validation.md（它是输入文档）
- 不跑 PoC-3（用户自己跑，W3 不代劳）
- 不发布 npm 包 / 不做 GitHub Actions CI（W11-W12 任务）

---

## 文件结构（W3 会创建或修改的）

```
docs/
├── poc-4-tracking.md                      (NEW Mon — Task 3.1.2，全周更新)
├── d2-raw-data.md                         (NEW Mon — Task 3.1.1，后台采样草稿)
├── w3-exit-review.md                      (NEW Fri — Task 3.5.5，W4 启动判据)
├── path-b-wrapper-design.md               (NEW Thu — 仅当 PoC-4 < 70% 时)
└── superpowers/plans/
    ├── 2026-05-04-wedge-w3.md             (THIS plan)
    ├── 2026-05-08-poc-4-results.md        (NEW Fri — Task 3.5.1)
    ├── 2026-05-08-d1-d2-research.md       (NEW Tue/Thu/Fri — Task 3.2.2 + 3.4.4 + 3.5.2)
    └── 2026-05-11-w4-conflict-v1.md       (NEW Wed/Thu/Fri — Task 3.3.3 + 3.4.3 + 3.5.3)

ARCHITECTURE.md                            (MODIFY Fri — 三处 🚧 锚点回填，Task 3.5.4)
```

---

## W4 预告（指针）

W4 = 冲突可见 v1 编码启动（PRODUCT.md §10.2 第一行：W3-W5 冲突检测 v1）。

具体 Day 1-5 任务在 `docs/superpowers/plans/2026-05-11-w4-conflict-v1.md`（W3 末交付）。W4 的第一行改动将是 migration 004：`processes` 表 + `conflicts` 表 DDL（或者，如果 PoC-4 < 70%，W4 Day 1-2 先做 Path B wrapper 设计）。
