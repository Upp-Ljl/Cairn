# 427 会话归档

> 日期：2026-04-27
> 分支：main，HEAD `bcb4fc1`
> 提交范围：`553d535..bcb4fc1`（今日 14 commits，含 W2 Day 1 全部任务）
> 阶段：W2 周一（v0.1 13 周计划第 2 周第 1 天）

---

## 1. 一句话总结

W2 计划 11 个任务（T2.1-T2.11）在 Day 1 一次性推完：接通楔 → dogfood 三件事 → 10 条 friction → 3 个 fix commit → 9 个新 acceptance 用例 → 文档同步，tests 从 76 升至 85，全绿。

---

## 2. 时间线

### 阶段一：W2 计划编写 + 校准（commits `553d535`..`22af311`）

早上先把 W2 执行计划写齐，再做两轮校准：

- `553d535` 写入 `CLAUDE.md`：push 方法、monorepo 结构、常见任务，确保跨会话一致性
- `ae374c4` 产出 `docs/superpowers/plans/2026-04-27-wedge-w2.md`：5 天任务列表（T2.1-T2.18）、exit criteria 9 条、kill criteria 兜底
- `22af311` 追加 Tech Stack + superpowers 技能 mapping 段——触发点是用户说"最好利用 superpowers 技能辅助"，sonnet subagent 补写了对应段落

关键决策：T2.12-T2.15（种子用户）按原计划排在 Fri；Day 1 只覆盖接通 + dogfood，未提前动种子用户任务。

### 阶段二：接通 + dogfood 三任务（commits `5d95cdc`..`6118962`，T2.1-T2.5）

1. **接通**（`5d95cdc` `.gitignore` 加 `.mcp.json`，本地手动创建 `.mcp.json`）：7 个工具首次出现在工具列表，三步验证（list/write/read）一次通过。
2. **T2.2 模板**（`e6ee28c`）：创建 `docs/wedge-friction-w1.md`，含字段说明，等 dogfood 填充。
3. **T2.3 dogfood - README 改动**（`97962a1`、`89b70da`）：
   - 修复安装 URL（把 `cairn` 小写改为 `Cairn` 大小写正确形式）+ 加"接通后快速验证"节
   - dogfood 中发现 friction #1（subagent 调不到楔）、#2（clean-tree checkpoint 无用）、#3（rewind 错误信息用内部术语）、#4（0 摩擦正面记录）、#5（工具名被 Markdown 链接化）
4. **T2.4 dogfood - scratchpad.ts JSDoc**（`92c2d9f`、`00ac8ae`）：
   - subagent 给 `putScratch`/`getScratch`/`listAllScratch` 加 JSDoc；dirty-tree vs clean-tree 对照实验
   - 记录 friction #6（dirty-tree 完全正常，对照）、#7（`updated_at` unix ms 不可读）、#8（subagent 与楔的隔层感）
5. **T2.5 dogfood - bugs + rewind.to e2e**（`6118962`）：
   - 故意搞坏文件 → checkpoint → 改为多版本乱码 → `rewind.to` → 字节级还原，验证核心卖点
   - 填入 5 条 bug（3 条 W2 必修，2 条进 P2），补 friction #9（rewind.to 正面确认）、#10（stash 捕获范围 ≠ 用户焦点）

关键决策点：dogfood 必须父 agent 亲自做（楔不下传 subagent），friction #8 揭示这不是限制而是产品定位，随即记入 README 新章节。

### 阶段三：Friction 分类 + 三个 W2 fix + 文档同步（commits `3fa561b`..`bcb4fc1`，T2.7-T2.11）

T2.7 分类：friction #2/#3/#7 = 必修（W2），#1/#4/#6/#9 = 不算 friction（正面），#5/#8/#10 = 文档化/进 P2。

T2.8 三个独立 fix commit，每个先写 failing test 再修：
- `3fa561b` — clean-tree checkpoint 返回 `warning` 字段；rewind 错误信息改为用户语言（bug #1 + #2）
- `2455770` — `scratchpad.list` 新增 `updated_at_iso` ISO 字符串字段（bug #3）+ 4 个边界 acceptance test

T2.10 在 `2455770` 同批加入：中文 key、大 blob（220 KB+）、非 git 仓库、并发写同 key。fix `3fa561b` 同批含 clean-tree acceptance test。mcp-server 9 → 18 tests。

T2.9 全套验证：daemon 67 + mcp-server 18 = 85，tsc clean 两包。

T2.11（`bcb4fc1`）：README 加"在多 Agent 工作流里使用"章节；`wedge-bugs-w1.md` 标记 3 条 W2 已修；`wedge-friction-w1.md` 状态列标注 W2 fixed commit SHA；W2 plan 末尾追加 EOD 进度总结。

---

## 3. 交付物清单

### 文档

- `docs/superpowers/plans/2026-04-27-wedge-w2.md` — W2 计划（含 Day 1 EOD 进度总结）
- `docs/wedge-friction-w1.md` — 10 条 friction 完整记录（`e6ee28c` 模板 → 后续 commits 填充）
- `docs/wedge-bugs-w1.md` — 5 条 bug，3 条标注 W2 已修 commit SHA（`bcb4fc1`）
- `packages/mcp-server/README.md` — 新增"接通后快速验证"节、"在多 Agent 工作流里使用"节、W1 限制更新（`97962a1`、`bcb4fc1`）
- `CLAUDE.md` — push 方法 + monorepo 约定 + 测试命令（`553d535`）

### 代码

- `3fa561b` — `packages/mcp-server/src/tools/checkpoints.ts`：`toolCreateCheckpoint` clean-tree 分支加 `warning` 字段；`toolRewindPreview` / `toolRewindTo` 错误信息重写
- `2455770` — `packages/mcp-server/src/tools/scratchpad.ts`：`toolListScratch` 返回增加 `updated_at_iso`
- `92c2d9f` — `packages/daemon/src/storage/repositories/scratchpad.ts`：`putScratch` / `getScratch` / `listAllScratch` 加 JSDoc

### 测试

- mcp-server acceptance：9 → 18 tests（+9 个）
  - `3fa561b` 含：clean-tree checkpoint warning test
  - `2455770` 含：中文 key、220 KB+ blob 分流、非 git 仓库、并发写、ISO 时间格式，共 5 个边界用例
  - 总计 `packages/mcp-server/tests/wedge-acceptance.test.ts`
- daemon 67 tests 不变

### 配置

- `.mcp.json`（本地，gitignored）— 首次接通楔，绝对路径指向 `packages/mcp-server/dist/index.js`
- `.gitignore` — 追加 `.mcp.json` 规则（`5d95cdc`），追加 `.cairn-push-token/` 规则（`86f7a44`，昨日）

---

## 4. 关键决策与对话回放

### 决策 1：W2 计划加 superpowers 技能 mapping 段

- 决策：在 W2 计划顶部明确列出每类任务对应的 superpowers 技能（`subagent-driven-development` / `test-driven-development` / `verification-before-completion` / `writing-plans`）
- 触发：用户说"最好利用 superpowers 技能辅助"，当时计划草稿没有这一段
- 用户态度：主动提出，属于计划体例要求
- 落地结果：`22af311` 追加，后续执行时子 agent 按技能类型路由任务

### 决策 2：接通流程"直接创建"

- 决策：我（Claude）直接 Write `.mcp.json` 写入绝对路径，不给步骤让用户手动做
- 触发：用户说"你直接创建"
- 用户态度：主动授权，不想看步骤
- 落地结果：接通一次成功，7 工具出现，三步验证通过；`.gitignore` 同步加规则（`5d95cdc`）

### 决策 3：dogfood 必须父 agent 亲自做

- 决策：楔工具不下传 subagent，dogfood friction 只有父 agent 才能感受到；subagent 承担的是"改代码"这段，不是"感知 checkpoint 是否好用"这段
- 触发：T2.4 派 subagent 加 JSDoc，发现 subagent 工具列表里没有 `cairn-wedge`
- 用户态度：接受（friction #1 记录为中级，符合产品定位）
- 落地结果：friction #8 文档化"两次 checkpoint 包夹一次代码改动"的 pattern；README 新增"在多 Agent 工作流里使用"章节

### 决策 4：clean-tree checkpoint 短期做文案 + warning，不做 git_head 兜底

- 决策：bug #1 的修复路径选"加 warning 字段 + 改错误信息"（option a），不做"git checkout HEAD -- files" 兜底（option b）
- 触发：dogfood 中 friction #2 严重度为高；W3 再决策长期路径
- 用户态度：接受（W3 决策，不在 W2 增加功能）
- 落地结果：`3fa561b`；bug #1 + #2 在 bugtracker 标注 W2 已修，README 限制说明更新

### 决策 5：subagent 调不到楔写入 README，不作为 bug 修

- 决策：friction #1 不进 bugtracker，定性为产品定位特性；长期在 daemon 主进程 + IPC 后解决
- 触发：dogfood 第一次发现；与"楔 = 宿主 Agent 本地工具"的 PRODUCT.md 定位一致
- 用户态度：接受
- 落地结果：README "在多 Agent 工作流里使用"节明确写"这是产品定位，不是 bug"

### 决策 6：T2.10 边界用例与 fix commit 合并提交

- 决策：`2455770` 把 scratchpad ISO 字段 fix 和 4 个边界 acceptance test 合并为一个 commit；`3fa561b` 把 clean-tree fix 和对应 acceptance test 合并
- 触发：测试直接覆盖 fix 的 regression；分开 commit 会让 bisect 变复杂
- 用户态度：隐性接受（计划里 T2.8 和 T2.10 没有强要求分开）
- 落地结果：mcp-server 一次从 9 升到 18，无中间状态

### 决策 7：种子用户暂缓，继续用楔做自己的开发

- 决策：T2.12-T2.15（种子用户）不在本周加速推进；等产品体验再完善后再发
- 触发：用户明示"先不必发散到用户，我们起码先自己有产品体验再说"
- 用户态度：主动决定，已口头确认
- 落地结果：T2.12-T2.15 留 Fri 位置，暂无具体行动；PRODUCT.md §10.1 10 周观察窗口尚未启动

---

## 5. 已知技术债 / 待办事项

### 楔级技术债（继承自 W1，DESIGN_STORAGE.md §17.1）

1. `checkpoints.label` 字段存了 `::stash:<sha>`（bug #5，进 P2，加 `backend_data` 列后迁出）
2. `mcp-server` 直接 import daemon `src/`（非 dist/ 或 exports map，需 pnpm workspaces 统一）
3. `packages/daemon/src/index.ts` 是空 placeholder

### 楔级 P2 推后（已记录，不在 W2 动）

- bug #4：`checkpoints.size_bytes` 总是 0（需要真实 snapshot backend）
- bug #5：`checkpoints.label` 被注入 `::stash:<sha>`（DESIGN_STORAGE.md §17.1）

### 文档化但未修

- friction #10：`rewind.preview` 暴露的"stash 捕获范围 ≠ 用户当前关注范围"（中级，三选一：paths 参数 / 标注 unchanged / 文档明示；短期 (c)，长期 (a)）

### W2 剩余任务

- T2.12 决定种子用户渠道（用户待拍板，当前倾向先缓）
- T2.13 邀请文案（3 个版本：朋友圈 / X 公开 / GitHub README）
- T2.14 实际发出邀请（3-5 人）
- T2.15 创建 `seed-user-tracking.md`
- T2.16 周末每日监控
- T2.17 紧急 hotfix 路径（如有种子用户 P0 bug）
- T2.18 周日晚写 W3 计划（用 superpowers:writing-plans）

### 长期决策留 W3

- clean-tree checkpoint 的 `git_head` 兜底回滚路径（friction #2 修补方案 b）

---

## 6. 数字

| 指标 | 数值 |
|---|---|
| 今日 commits | 14（`553d535..bcb4fc1`） |
| 总 commits 自项目起（`ae4fe7d..HEAD`） | 47 |
| daemon tests | 67，全绿 |
| mcp-server tests | 18（W2 Day 1 +9，从 9 升至 18），全绿 |
| 总 tests | 85 |
| friction 总数 | 10 条（3 W2 已修，2 正面记录不算 friction，1 文档化，4 进 P2 / 不修） |
| bugs 总数 | 5 条（3 W2 已修，2 进 P2） |
| 累计新增代码行（自项目起） | +17,363 / -40 |
| 文档新增（今日） | CLAUDE.md（~100 行）+ W2 plan（~420 行）+ friction（~121 行）+ bugs（~62 行）+ README 新增节（~30 行）|

---

## 7. 接下来可以接着做的具体事

**W2 剩余（按计划编号）**：

1. **T2.12 决定种子用户渠道** — 朋友私信 / X DM / GitHub Issue / 朋友圈 / Discord；用户当前倾向先缓。不缓的话需要用户拍板选哪几个渠道
2. **T2.13 邀请文案** — 我起草 3 个版本（朋友圈中文 / X 英文 / GitHub README 段），用户审稿。文案存 `docs/seed-invite-draft.md`（临时文件）
3. **T2.14 实际发出** — 对每个渠道发 1 条，目标 3-5 人；记录发出时间 + 渠道
4. **T2.15 创建 `seed-user-tracking.md`** — 化名 / 渠道 / 发出时间 / 装上时间 / 初次反馈
5. **T2.16 周末每日监控** — 检查各渠道反馈，有则立即回复，无则记"无反馈"
6. **T2.17 紧急 hotfix 路径** — 单 commit 直推 main，通知受影响用户
7. **T2.18 周日晚 W3 计划编写** — 用 superpowers:writing-plans 技能；决策依据是 W2 收集的 friction + 装机率 + 工具调用分布

**用户当前明示倾向**：继续用楔做 cairn 自己的开发，暂缓对外发种子用户。注意：PRODUCT.md §10.1 的 10 周观察窗口越早启动越早收尾，§17.3 成功判据（3 个非作者连续 2 周）deadline 是 W3 末，若种子用户 0 装机则 §17.3 在 W3 末不可达。

**可立即继续（不需要用户决策）**：
- 用楔做任何 cairn 内部改动（commit JSDoc / 补文档 / 加测试）的 checkpoint + rewind 练手
- 准备 T2.18 的 W3 计划草稿输入材料（friction top 3、bug 列表、当前 exit criteria 达成情况）

---

## 8. 反思

**做对的地方**：subagent rotation 依然有效——T2.4 JSDoc、三个 fix commit、边界 acceptance 各用 fresh subagent 保证了聚焦度，没有因上下文积累引起的偏移。TDD 严格执行（先写 failing test 再 fix）让 `3fa561b` 和 `2455770` 都在修复的同时零回归。Friction 即时记录的纪律——每次 dogfood 结束立刻写进文件，而不是"回头再整理"——让 10 条 friction 的分类在 T2.7 只花了很短时间。

**可以更好的地方**：push 阶段两次遭遇 TLS 抖动（`read: connection reset` / `SSL error`），重试才过；下次应该在确认 push 成功后再关会话，不要假设第一次 push 一定成功。`docs/rewind-demo.md` 是 T2.5 dogfood 时故意搞坏的临时文件，dogfood 结束后需要人工 `git rm` 清理，这一步没有被自动化也没有在计划里预留——以后 dogfood 临时文件应该在任务单里加"清理步骤"。

**一个非显而易见的 lesson**：楔工具不下传 subagent 这件事起初看起来是限制（subagent-driven dogfood 用不到楔），但 T2.4 的实验揭示它是产品定位的准确揭示：楔是"父 agent 对自身工作状态的本地备份"，和"把工作分发给 subagent"是两个不同层次的关注。subagent 做代码改动，父 agent 做回滚决策——两者的工具集本应分离。这次 dogfood 让我们把这个设计意图从隐性变成了 README 里的显性文字，比任何架构文档都更有说服力。

---

## 9. 索引

### 计划

- [`docs/superpowers/plans/2026-04-27-wedge-w2.md`](docs/superpowers/plans/2026-04-27-wedge-w2.md) — W2 验证计划（含 Day 1 EOD 进度总结）
- [`docs/superpowers/plans/2026-04-23-wedge-w1.md`](docs/superpowers/plans/2026-04-23-wedge-w1.md) — W1 楔计划（路径 B + 三补丁，已执行完毕）

### 修复 commit

- `3fa561b` — clean-tree checkpoint UX（bug #1 + #2）
- `2455770` — scratchpad.list ISO timestamp + 4 边界 acceptance tests（bug #3）

### 文档（今日新增 / 重大修改）

- [`packages/mcp-server/README.md`](packages/mcp-server/README.md) — 接通验证节 + 多 Agent 工作流节 + W1 限制更新
- [`docs/wedge-friction-w1.md`](docs/wedge-friction-w1.md) — 10 条 friction 完整记录
- [`docs/wedge-bugs-w1.md`](docs/wedge-bugs-w1.md) — 5 条 bug，3 条 W2 已修

### 配置

- [`CLAUDE.md`](CLAUDE.md) — push 方法 + monorepo 约定（跨会话必读）

### 楔归档

- [`426归档.md`](426归档.md) — 昨日归档（P1 + W1 楔 MVP，32 commits）
- [`427归档.md`](427归档.md) — 本文件（W2 Day 1，14 commits）
